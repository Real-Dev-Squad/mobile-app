import { StyleSheet } from 'react-native';

export const TabViewStyle = StyleSheet.create({
  tab_bar: {
    paddingHorizontal: 5,
    padding: 10,
    backgroundColor: '#fff',
    borderTopWidth: 3,
    borderColor: 'grey',
  },
  tab_icon: {
    height: 20,
    width: 20,
  },
});
